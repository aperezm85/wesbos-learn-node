extends layout

block content 
  .inner
    h2 Edit Your Account
    form(action="/account")
      label(for="name") Name
      input(type="text" name="name" value=user.name)
      label(for="email")
      input(type="email" name="emaik" value=user.email)
      input.button(type="submit" value="Update my Account")